openapi: 3.0.3
info:
  title: AgentNexus Gateway Agent API
  version: 1.0.0
  description: >-
    Agent Control Plane entrypoints for run onboarding, messages, and
    confirmations (vendor-neutral contract v1, ELC-NEXUS-1 candidate).
    Trusted identity is derived from verified credentials only and never
    travels in request JSON.
paths:
  /v1/agent/runs:
    post:
      operationId: createAgentRun
      summary: Create a Gateway Agent run.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRunRequest'
      responses:
        '200':
          description: Created run.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunResponse'
  /v1/agent/runs/{id}/messages:
    post:
      operationId: appendAgentRunMessage
      summary: Append a user or system message to a run.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppendMessageRequest'
      responses:
        '200':
          description: Updated run.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunResponse'
  /v1/agent/runs/{id}/confirmations:
    post:
      operationId: confirmAgentRunCheckpoint
      summary: Confirm or reject a human-in-the-loop checkpoint.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmCheckpointRequest'
      responses:
        '200':
          description: Updated checkpoint state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckpointResponse'
components:
  schemas:
    RequestEnvelope:
      type: object
      required:
        - request_id
      properties:
        request_id:
          type: string
          minLength: 1
          maxLength: 128
        trace_id:
          type: string
          maxLength: 128
      description: >-
        Correlation only. Trusted identity is derived from verified
        credentials and never accepted from request JSON. Unknown request
        fields are rejected server-side (OpenAPI 3.0 cannot express
        additionalProperties false across allOf composition; the Go SDK rules
        are normative).
    CreateRunRequest:
      allOf:
        - $ref: '#/components/schemas/RequestEnvelope'
        - type: object
          required:
            - intent
          properties:
            intent:
              type: string
            attachments:
              type: array
              items:
                $ref: '#/components/schemas/ArtifactRef'
    AppendMessageRequest:
      allOf:
        - $ref: '#/components/schemas/RequestEnvelope'
        - type: object
          required:
            - role
            - content
          properties:
            role:
              type: string
              enum:
                - user
                - assistant
                - system
            content:
              type: string
            attachments:
              type: array
              items:
                $ref: '#/components/schemas/ArtifactRef'
    ConfirmCheckpointRequest:
      allOf:
        - $ref: '#/components/schemas/RequestEnvelope'
        - type: object
          required:
            - checkpoint_id
            - decision
          properties:
            checkpoint_id:
              type: string
            decision:
              type: string
              enum:
                - approved
                - denied
                - narrowed
                - instructed
            comment:
              type: string
    AgentRunResponse:
      type: object
      required:
        - run_id
        - status
      properties:
        run_id:
          type: string
        task_run_id:
          type: string
        status:
          type: string
        trace_id:
          type: string
    CheckpointResponse:
      type: object
      required:
        - checkpoint_id
        - status
      properties:
        checkpoint_id:
          type: string
        status:
          type: string
    ArtifactRef:
      type: object
      required:
        - artifact_id
      properties:
        artifact_id:
          type: string
        source_hash:
          type: string
